// jQuery === $
// use standard CSS selectors .klass && #eyed && <h1>
//
console.log($('.klass'));
console.log($('#eyed'));
console.log($('h1'));
//
// can be set to a variable
var domKlass = $('.klass');
console.log(domKlass);
